---
- hosts : 
  gather_facts: false
  become: true

  tasks:
          - name: Create a QA instance
            
          - name: Install required packages 
          - name: Dowmload the nexus repo
          - name : Copy the nexus repo to the QA instance
